<style>
	.login {
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: center;
  align-items: center;
  flex-direction: column;
  background-color:black; }
  .login .login__heading {
    color: var(--text-color);
    font-size: 3rem;
    margin-bottom: 2rem; }
  .login #login__form {
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column; }
    .login #login__form input {
      width: 100%;
      height: 3rem;
      border: none;
      border-bottom: 1px solid white;
      background-color: transparent;
      color: var(--text-color);
      font-size: 1.5rem;
      margin: 1rem 0;
      padding: 10px;
      outline: none; }
      .login #login__form input::placeholder {
        color: var(--text-color);
        font-size: 1.3rem;
        text-align: center; }
      .login #login__form input:focus {
        outline: none; }
    .login #login__form button {
      width: 100%;
      height: 3rem;
      border: none;
      border-radius: 5px;
      background-color: green;
      color: var(--text-color);
      font-size: 1.5rem;
      margin: 1rem 0;
      outline: none;
      cursor: pointer; }
      .login #login__form button:hover {
        background-color: green; }
  .login a {
    color: var(--text-color);
    text-decoration: none;
    margin-top: 2rem;
    cursor: pointer; }
    .login a:hover {
      text-decoration: underline; }
    .login a:hover, .login a:focus, .login a:active, .login a:visited, .login a:link {
      color: var(--text-color);
      outline: none; }
  .login .links {
    margin-top: 1rem;
    color: var(--text-color); }
    .login .links a {
      color: var(--text-color);
      text-decoration: none;
      cursor: pointer; }
      .login .links a:hover {
        text-decoration: underline; }
      .login .links a:hover, .login .links a:focus, .login .links a:active, .login .links a:visited, .login .links a:link {
        color: var(--text-color);
        outline: none; }
        @import url("https://fonts.googleapis.com/css?family=Open+Sans:400,400i,700");
@keyframes scale {
  from {
    transform: scale(0); }
  to {
    transform: scale(1); } }

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box; }

.modal {
  display: none;
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.2);
  z-index: 300; }
  .modal .modal-content {
    margin: 20% auto;
    max-width: 500px;
    background-color: #fff;
    padding: 20px;
    animation: scale 0.5s ease;
    border-radius: 10px;
    box-shadow: 6.7px 6.7px 5.3px rgba(0, 0, 0, 0.02), 22.3px 22.3px 17.9px rgba(0, 0, 0, 0.03), 100px 100px 80px rgba(0, 0, 0, 0.05); }
    .modal .modal-content #close-modal {
      float: right;
      font-size: 1.2rem;
      font-weight: bold;
      cursor: pointer; }
    .modal .modal-content .modal-body {
      margin-top: 5px;
      display: flex;
      justify-content: flex-start;
      align-items: center; }
      .modal .modal-content .modal-body .social {
        width: 5rem;
        height: 5rem;
        cursor: pointer; }
      .modal .modal-content .modal-body .twitter {
        width: 5.6rem;
        height: 5.6rem; }

@media screen and (max-width: 500px) {
  .modal .modal-content {
    margin-top: 50%; } }
    @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800;900&family=Red+Hat+Display:wght@300;400;500&display=swap");
* {
  -webkit-user-select: none;
  -webkit-user-drag: none;
  -moz-user-select: none;
  -ms-user-select: none;
  -o-user-select: none;
  -webkit-box-sizing: border-box;
  -moz-box-sizing: border-box;
  user-select: none;
  padding: 0;
  margin: 0;
  box-sizing: border-box;
  font-family: "Red Hat Display", sans-serif;
  font-family: "Poppins", sans-serif; }

:root {
  --background-color: #040404;
  --logo-color: #1ed15e;
  --text-color: #fff;
  --heading-font-family: "Poppins", sans-serif;
  --text-font-family: "Red Hat Display", sans-serif; }

body {
  position: relative;
  width: 100%;
  height: 100%; }

body::-webkit-scrollbar {
  display: none; }

main {
  width: 100vw;
  height: 100vh;
  min-height: 100vh; }

</style>

<div class="login">
	<h1 class="login__heading"><%= title %></h1>
	<form id="login__form" action="/users/create-session" method="post">
		<input
			type="email"
			name="email"
			placeholder="Enter Email"
			autocomplete="off"
			required
		/>
		<input
			type="password"
			name="password"
			placeholder="Enter Password"
			autocomplete="off"
			required
		/>
		<button class="login__btn" type="submit">Login</button>
	</form>
	<div class="links">
		<a href="/users/signup"> Register </a>&ensp;|&ensp;<a href="/"> Home </a>
	</div>
	<a href="/forgot-password"> Forgot Password? </a>
</div>
